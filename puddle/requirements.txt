Django>=5.2,<6.0
Pillow>=10.0,<11.0
django-tailwind>=3.7,<4.0


# Optional: For crispy forms if used
django-crispy-forms>=2.0,<3.0

# Remove built-in Django apps from requirements (they are not installable packages)